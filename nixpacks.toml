[phases.setup]
nixPkgs = ["ffmpeg", "python311", "nodejs_20", "nodePackages.npm"]

[phases.install]
cmds = [
  "python3 -m pip install -r requirements.txt",
  "cd web/frontend && npm install"
]

[phases.build]
cmds = [
  "cd web/frontend && npm run build"
]

[start]
cmd = "python3 main.py"
